# requirements.txt — log-analyzer-anssi
# Python 3.11+ requis (ARM64 / Raspberry Pi 4 compatible)

# ─── LangGraph + LangChain ───────────────────────────────────────────────────
langgraph>=0.2.0,<1.0.0
langchain>=0.2.0,<1.0.0
langchain-community>=0.2.0,<1.0.0

# ─── FastAPI ─────────────────────────────────────────────────────────────────
fastapi>=0.111.0,<1.0.0
uvicorn[standard]>=0.30.0,<1.0.0
pydantic>=2.7.0,<3.0.0
pydantic-settings>=2.3.0,<3.0.0

# ─── PostgreSQL async ────────────────────────────────────────────────────────
asyncpg>=0.29.0,<1.0.0
sqlalchemy[asyncio]>=2.0.30,<3.0.0
alembic>=1.13.0,<2.0.0
pgvector>=0.3.0,<1.0.0

# ─── HTTP client (Ollama async, ntfy, tests) ─────────────────────────────────
httpx>=0.27.0,<1.0.0
aiohttp>=3.9.0,<4.0.0

# ─── I/O asynchrone ──────────────────────────────────────────────────────────
aiofiles>=23.2.0,<25.0.0

# ─── Cryptographie (HMAC intégrité ANSSI) ────────────────────────────────────
cryptography>=42.0.0,<44.0.0

# ─── Sécurité XML (parsing Sysmon — B314 ANSSI) ─────────────────────────────
defusedxml>=0.7.0,<1.0.0

# ─── Logging structuré ───────────────────────────────────────────────────────
structlog>=24.1.0,<26.0.0

# ─── Linting / type checking (dev) ───────────────────────────────────────────
ruff>=0.4.0,<1.0.0
mypy>=1.10.0,<2.0.0

# ─── Tests ───────────────────────────────────────────────────────────────────
pytest>=8.2.0,<9.0.0
pytest-asyncio>=0.23.0,<1.0.0
pytest-cov>=5.0.0,<7.0.0

# ─── Optionnel : capture réseau (tcpdump fallback) ───────────────────────────
# scapy>=2.5.0,<3.0.0  # Décommenter si capture pcap requise
